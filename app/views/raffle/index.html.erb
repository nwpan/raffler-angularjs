<h1>Raffler</h1>
<div ng-controller="RaffleCtrl">
  <form ng-submit="addEntry()"><!-- newEntry is object of this form, name is the attribute of this field. -->
    <input type="text" ng-model="newEntry.name">
    <input type="submit" value="Add">
  </form>
  <ul>
    <li ng-repeat="entry in entries">
      {{entry.name}}
      <span ng-show="entry.winner" ng-class="{highlight: entry == lastWinner}" class="winner">WINNER</span>
      <br />
      <button ng-click="destroyEntry($index, entry)">X</button>
    </li>
  </ul>
  <button ng-click="drawWinner()">Draw Winner</button>
</div>
